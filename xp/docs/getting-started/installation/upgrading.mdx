---
slug: /upgrade
sidebar_position: 2
sidebar_label: Upgrade
title: Upgrade Level Up XP
description: Upgrade XP and XP+ while keeping the plugins compatible.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Upgrade Level Up XP

Upgrade XP by itself, or upgrade XP and XP+ together when both are installed.

## Before you upgrade

Read the [upgrade notes](./upgrade-notes/) for any extra steps and the [release notes](/xp/release-notes) for details about each version.

## Get the updates

<span id="xp"></span>
<span id="xpplus"></span>

| Plugin | Code name | How to obtain |
| --- | --- | --- |
| XP (required) | `block_xp` | Download the ZIP file from the [Level Up website](https://www.levelup.plus/xp/#packages), [CAMP](https://camp-registry.org/plugin/block_xp.html), or the [Moodle plugins directory](https://moodle.org/plugins/block_xp). You can also use the Git method below. |
| XP+ (if installed) | `local_xp` | Download `local_xp.zip` from your licence download link. If you lost the link, use the [licence recovery tool](https://levelup.plus/recover). Your licence determines which versions you can download. |

<span id="note-xpplus-users"></span>

:::warning

If you use XP+, obtain updates for both plugins before you start. XP and XP+ must have the same major version. Do not continue the Moodle upgrade until you update the files for both plugins.

:::

## Upgrade

Choose the method that you use to manage the plugin files.

<Tabs>
  <TabItem value="zip" label="ZIP upload" default>
    <Admonition type="note">
      This method is not available on all Moodle sites. Its availability depends on the site configuration and hosting provider.
    </Admonition>

    1. Sign in to your Moodle site and open Site administration > Plugins > Install plugins.
    2. Upload the XP ZIP file.
    3. If XP+ is installed, return to Site administration > Plugins > Install plugins and upload `local_xp.zip`.
    4. Follow the Moodle upgrade process.
  </TabItem>

  <TabItem value="server" label="Server files">
    1. Replace `[public]/blocks/xp` with the contents of the XP ZIP file.
    2. If XP+ is installed, replace `[public]/local/xp` with the contents of `local_xp.zip`.
    3. Confirm that each plugin folder contains a `version.php` file.
    4. Open Site administration > Notifications and follow the Moodle upgrade process.
  </TabItem>

  <TabItem value="git" label="Git">
    This method upgrades XP only.

    1. From the Moodle root directory, go to `[public]/blocks/xp` or `blocks/xp`.
    2. Fetch the release tags from the [official XP repository](https://github.com/FMCorz/moodle-block_xp).
    3. Check out the latest release tag.
    4. If XP+ is installed, replace `[public]/local/xp` with the contents of `local_xp.zip`.
    5. Open Site administration > Notifications and follow the Moodle upgrade process.

    The following commands fetch the release tags from the official repository and check out the latest tag.

    ```sh
    cd public/blocks/xp 2>/dev/null || cd blocks/xp || exit 1
    git fetch --tags
    git checkout "$(git tag --list 'v*' --sort=-v:refname | head -n 1)"
    ```
  </TabItem>
</Tabs>

After the upgrade, open Site administration > Plugins > Plugins overview and confirm the installed versions.
